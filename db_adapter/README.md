# Database Adapter Example

This example demonstrates how a model can serve as a simple database adapter,
allowing the simulation to query a database.

## Usage

To start the database server container run:

```bash
docker compose up -d
```

To run the example run:

```bash
cargo run
```

To start the database server container run:

```
docker compose down -v
```